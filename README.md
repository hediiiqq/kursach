it's just my homework in university 
## Running the Project

To run the project, update the database connection string in the `appsettings.json` file.

The project uses:
- PostgreSQL
- .NET 10
- ASP.NET MVC
